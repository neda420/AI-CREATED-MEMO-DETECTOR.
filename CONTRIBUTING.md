## Contributing

1. Fork the repo and create a feature branch.
2. Run `npm ci`.
3. Verify with `npm run typecheck`, `npm run lint`, `npm run test`, and `npm run build`.
4. Open a PR with a clear summary and screenshots for UI changes. 

This project is intentionally browser-only: do not add backend services, API routes, or analytics that capture user text.
